global:
  scrape_interval: 15s # A cada 15 segundos ele coleta os dados

scrape_configs:
  - job_name: 'node_exporter'
    static_configs:
      - targets: ['node-exporter:9100'] # Aponta para o container do Node Exporter